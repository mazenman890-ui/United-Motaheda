// Retired: dev overlay that read stats from the deleted `optimizedCatalogApi`.
// The surviving catalog stack does not need this overlay; if a perf monitor is
// wanted later, build one against `shopperCatalogApi`'s `pageCache` instead.
// Safe to delete this file.
export {};
