// Retired: companion hook to `services/optimizedCatalogApi.ts`, which queried a
// non-existent DB schema. Use `useCatalogProductSearch` from
// `app/hooks/useCatalogProductSearch.ts` (worker-backed, server-schema-correct)
// for catalog browsing instead. Safe to delete this file.
export {};
