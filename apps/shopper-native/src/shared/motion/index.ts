/**
 * @/shared/motion — reusable animation primitives.
 *
 * Cross-cutting motion building blocks shared across features so screens
 * compose premium motion from a vetted set instead of re-deriving it.
 */

export { PressableScale } from "./PressableScale";
export type { PressableScaleProps } from "./PressableScale";
export { RTL_ANDROID, pagerProgress, pagerOffset } from "./rtlPager";
